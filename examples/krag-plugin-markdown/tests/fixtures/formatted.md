# Formatted Text

This paragraph has **bold text**, *italic text*, and `inline code`.

Here's some __bold with underscores__ and _italic with underscores_.

## Code Example

```python
def hello():
    print("world")
```

The code block above should be removed during text extraction.
